# ── PROD environment — set these in Vercel dashboard, NOT in a file ──────────
# Vercel: Project → Settings → Environment Variables → Production

# Your Render.com backend URL
NEXT_PUBLIC_API_URL=https://<your-service>.onrender.com/api
